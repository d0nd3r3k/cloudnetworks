
header#header
  if (req.isAuthenticated())
    #user(data-id=req.user.id)
    .navbar.navbar-fixed-top(role='navigation')
      .navbar-inner
        .container
          a.brand(href='/') Cloudnetworks
          .nav-collapse.collapse
            ul.nav
           
            ul.nav.pull-right
              if (req.isAuthenticated())
                li
                  a(href="/boxes", title="Boxes") List Boxes  
                li
                  a(href="/boxes/new", title="Add New Box") Add New Box  
                li
                  a(href="/users/"+req.user.id, title="Profile") #{req.user.name}  
                li
                  a(href="/logout", title="logout") Logout
                
                    
              else
                li
                  a(href="/login", title="Login") Login
      